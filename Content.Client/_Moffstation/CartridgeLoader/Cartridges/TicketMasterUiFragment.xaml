<cartridges:TicketMasterUiFragment
    xmlns="https://spacestation14.io"
    xmlns:cartridges="clr-namespace:Content.Client._Moffstation.CartridgeLoader.Cartridges"
    xmlns:customControls="clr-namespace:Content.Client.Administration.UI.CustomControls"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    Orientation="Vertical"
    HorizontalExpand="True"
    VerticalExpand="True"
    Margin="5">

    <!-- Main container that fills the entire PDA screen -->
    <BoxContainer Orientation="Vertical"
                  HorizontalExpand="True"
                  VerticalExpand="True">

        <!-- Header with app title, new ticket and history button -->
        <controls:StripeBack MinSize="48 48"
                             VerticalExpand="False">

            <BoxContainer Orientation="Horizontal"
                          HorizontalExpand="True"
                          Margin="0">

                <!-- change texture -->
                <TextureRect Name="AppIcon"
                             TexturePath="/Textures/Interface/Nano/ntlogo.svg.png"
                             Stretch="KeepAspectCentered"
                             VerticalAlignment="Center"
                             MinSize="32 32"
                             Margin="8 0 0 0" />
                <Label Text="{Loc ticket-master-title}"
                       StyleClasses="LabelHeading"
                       HorizontalExpand="True"
                       Margin="8 0"
                       VerticalAlignment="Center" />
                <Button Name="NewTicketButton"
                        Text="{Loc ticket-master-new-ticket}"/>
                <Button Name="TicketHistoryButton"
                        Text = "{Loc ticket-master-history}"/>
            </BoxContainer>

        </controls:StripeBack>

    </BoxContainer>

</cartridges:TicketMasterUiFragment>
