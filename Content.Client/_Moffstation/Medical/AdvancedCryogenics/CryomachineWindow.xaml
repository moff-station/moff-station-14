<controls:FancyWindow xmlns="https://spacestation14.io"
               xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
               xmlns:health="clr-namespace:Content.Client.HealthAnalyzer.UI"
               xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
               xmlns:cryogenics="clr-namespace:Content.Client.Medical.Cryogenics"
               MinSize="250 300"
               Resizable="True">
    <BoxContainer Name="Sections"
                  Orientation="Horizontal"
                  Margin="10">

        <BoxContainer Name="CryoSection"
                      VerticalExpand="True"
                      Orientation="Vertical"
                      MinWidth="250"
                      MaxWidth="250">

            <!-- Flavor text -->
            <BoxContainer Orientation="Horizontal"
                          SeparationOverride="10"
                          Margin="8 0 0 8">
                <TextureRect StyleClasses="NTLogoDark"
                             VerticalExpand="True"
                             Stretch="KeepAspectCentered"
                             SetSize="32 32"/>
                <BoxContainer Orientation="Vertical"
                              SeparationOverride="-4">
                    <Label Text="{Loc 'cryomachine-window-product-name'}"
                           StyleClasses="FontLarge"/>
                    <Label Text="{Loc 'cryomachine-window-product-subtitle'}"
                           StyleClasses="LabelSubText"/>
                </BoxContainer>
            </BoxContainer>

            <!-- Atmos info -->
            <BoxContainer Orientation="Horizontal"
                          SeparationOverride="20"
                          Margin="0 0 0 4">

                <!-- Pressure -->
                <BoxContainer Orientation="Vertical">
                    <Label Text="{Loc 'cryomachine-window-pressure-text'}"
                           StyleClasses="LabelSubText"/>
                    <Label Name="Pressure"/>
                </BoxContainer>

                <!-- Temperature -->
                <BoxContainer Orientation="Vertical">
                    <Label Text="{Loc 'cryomachine-window-temperature-text'}"
                           StyleClasses="LabelSubText"/>
                    <Label Name="Temperature"/>
                </BoxContainer>

            </BoxContainer>

            <!-- Gas mix -->
            <Control Margin="0 0 0 22">
                <controls:SplitBar Name="GasMixChart"
                                   MinHeight="8"
                                   MaxHeight="8"/>
            </Control>

            <!-- Warnings & status -->
            <BoxContainer Orientation="Vertical"
                          VerticalExpand="True"
                          HorizontalExpand="True"
                          Align="Center"
                          Margin="0 0 0 14"
                          SeparationOverride="20">

                <!-- Pressure Warning -->
                <PanelContainer Name="LowPressureWarning"
                                Visible="False"
                                HorizontalExpand="True">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BorderThickness="1" BorderColor="orange"/>
                    </PanelContainer.PanelOverride>

                    <BoxContainer Orientation="Vertical"
                                  Margin="6">
                        <Label Text="{Loc 'cryomachine-window-warning-header'}"
                               FontColorOverride="orange"
                               Align="Center"/>
                         <RichTextLabel Text="{Loc 'cryomachine-window-low-pressure-warning'}"/>
                    </BoxContainer>
                </PanelContainer>

                <!-- Temperature Warning -->
                <PanelContainer Name="HighTemperatureWarning"
                                Visible="False"
                                HorizontalExpand="True">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BorderThickness="1" BorderColor="orange"/>
                    </PanelContainer.PanelOverride>

                    <BoxContainer Orientation="Vertical"
                                  Margin="6">
                        <Label Text="{Loc 'cryomachine-window-warning-header'}"
                               FontColorOverride="orange"
                               Align="Center"/>
                        <RichTextLabel Text="{Loc 'cryomachine-window-high-temp-warning'}"/>
                    </BoxContainer>
                </PanelContainer>

            </BoxContainer>

                <!-- Status Checklist -->
            <BoxContainer Orientation="Vertical">
                <BoxContainer Orientation="Horizontal"
                              SeparationOverride="8">
                    <Label Text = "{Loc 'cryomachine-window-status'}"/>
                    <Label Name="StatusLabel"
                           Text="{Loc 'cryomachine-status-no-capsule'}"
                           FontColorOverride="Orange"/>
                </BoxContainer>

                <GridContainer Columns="2"
                               HSeparationOverride="0"
                               VSeparationOverride="6"
                               Margin="6 3 0 0">
                    <Label Text="⋄"
                           StyleClasses="LabelSubText"/>
                    <Label Name="PressureCheck"
                           Text="{Loc 'cryomachine-window-checklist-pressure'}"
                           StyleClasses="LabelSubText"/>
                    <Label Text="⋄"
                           StyleClasses="LabelSubText"/>
                    <Label Name="ChemicalsCheck"
                           Text="{Loc 'cryomachine-window-checklist-chemicals'}"
                           StyleClasses="LabelSubText"
                           FontColorOverride="Orange"/>
                    <Label Text="⋄"
                           StyleClasses="LabelSubText"/>
                    <Label Name="TemperatureCheck"
                           Text="{Loc 'cryomachine-window-checklist-temperature'}"
                           StyleClasses="LabelSubText"/>
                </GridContainer>
            </BoxContainer>

            <!-- Reagents -->
            <Control HorizontalExpand="True"
                     MinHeight="30">
                <Label Name="NoBeakerText"
                       Text="{Loc 'cryomachine-window-no-beaker'}"
                       FontColorOverride="Gray"
                       VerticalExpand="True"
                       VAlign="Center"/>
                <cryogenics:BeakerBarChart Name="ChemicalsChart"
                                           HorizontalExpand="True"
                                           VerticalExpand="True"/>
            </Control>

            <!-- Buttons -->
            <BoxContainer Orientation="Vertical"
                          Margin="-2 2 -2 0">
                <BoxContainer Orientation="Horizontal">
                    <Button Name="Inject1"
                            Text="{Loc 'cryo-pod-window-inject-1u'}"
                            Disabled="True"
                            HorizontalExpand="True"
                            StyleClasses="OpenBoth"/>
                    <Button Name="Inject5"
                            Text="{Loc 'cryo-pod-window-inject-5u'}"
                            Disabled="True"
                            HorizontalExpand="True"
                            StyleClasses="OpenBoth"/>
                    <Button Name="Inject10"
                            Text="{Loc 'cryo-pod-window-inject-10u'}"
                            Disabled="True"
                            HorizontalExpand="True"
                            StyleClasses="OpenBoth"/>
                    <Button Name="Inject20"
                            Text="{Loc 'cryo-pod-window-inject-20u'}"
                            Disabled="True"
                            HorizontalExpand="True"
                            StyleClasses="OpenBoth"/>
                    <Button Name="EjectBeakerButton"
                            Text="{Loc 'cryo-pod-window-eject-beaker'}"
                            Disabled="True"
                            StyleClasses="OpenBoth"/>
                </BoxContainer>
            </BoxContainer>

        </BoxContainer>

        <BoxContainer Name="PatientSection"
                      VerticalExpand="True"
                      Orientation="Vertical"
                      MinWidth="250"
                      MaxWidth="250">

            <!-- Capsule Infos -->
            <BoxContainer Orientation="Horizontal">

                <PanelContainer VerticalExpand="True">

                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1B1B1E"/>
                    </PanelContainer.PanelOverride>
                    <BoxContainer HorizontalAlignment="Center" VerticalAlignment="Center">
                        <SpriteView Name="CapsuleSprite" Scale="2 2"/>
                    </BoxContainer>
                </PanelContainer>



            </BoxContainer>


            <BoxContainer Orientation="Horizontal">
                <Button Name="JumpStartBrainButton" Text="Activate Brain"/>
                <Button Name="DetachCapsuleButton" Text="Detach Capsule"/>
            </BoxContainer>
        </BoxContainer>


    </BoxContainer>


</controls:FancyWindow>
