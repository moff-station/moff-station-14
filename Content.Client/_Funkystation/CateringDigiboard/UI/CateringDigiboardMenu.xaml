<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      xmlns:graphics="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      Title="{Loc 'catering-digiboard-title'}"
                      MinSize="480 720"
                      SetSize="480 720">
    <BoxContainer Orientation="Vertical">
        <PanelContainer Margin="0 0 0 8">
            <PanelContainer.PanelOverride>
                <graphics:StyleBoxFlat BackgroundColor="#444550" />
            </PanelContainer.PanelOverride>
            <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="8 4">
                <Label Text="{Loc 'catering-digiboard-menu-label'}" VerticalAlignment="Center" HorizontalExpand="True" />
                <Button Name="AddCategoryButton" Text="{Loc 'catering-digiboard-add-category-button'}" Margin="0 0 4 0" />
                <Button Name="AddItemButton" Text="{Loc 'catering-digiboard-add-item-button'}" />
            </BoxContainer>
        </PanelContainer>

        <ScrollContainer HorizontalExpand="True" VerticalExpand="True">
            <BoxContainer Name="ItemList" Orientation="Vertical" />
        </ScrollContainer>

        <PanelContainer StyleClasses="LowDivider" Margin="0 8 0 8" />

        <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="12 0 10 10">
            <Label Text="{Loc 'catering-digiboard-print-label'}" VerticalAlignment="Center" HorizontalExpand="True" />
            <Button Name="PrintOneButton" Text="{Loc 'catering-digiboard-print-one-button'}" Margin="0 0 4 0" />
            <Button Name="PrintFiveButton" Text="{Loc 'catering-digiboard-print-five-button'}" Margin="0 0 4 0" />
            <Button Name="PrintTenButton" Text="{Loc 'catering-digiboard-print-ten-button'}" />
        </BoxContainer>

        <BoxContainer Orientation="Vertical">
            <PanelContainer StyleClasses="LowDivider" />
            <BoxContainer Orientation="Horizontal" Margin="10 4 10 4" VerticalAlignment="Bottom">
                <Label Text="NT CATERING DIGI-BOARD" StyleClasses="WindowFooterText" />
                <Label Text="SERVICE SYS" StyleClasses="WindowFooterText" HorizontalAlignment="Right" HorizontalExpand="True" Margin="0 0 5 0" />
                <TextureRect StyleClasses="NTLogoDark" Stretch="KeepAspectCentered" VerticalAlignment="Center" HorizontalAlignment="Right" SetSize="16 16"/>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
